apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: network
  namespace: network
spec:
  sourceRepos:
    - "*"
  destinations:
    - namespace: "*"
      server: https://kubernetes.default.svc
